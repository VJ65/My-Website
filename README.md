# Syed Wajeeh Ul Hassan — Portfolio

Static site (no build step). Publish directory: `.`

Pages: index, about, services (?s=key), industries (?i=key), projects, blog, contact, projections, ab-test, admin → dashboard.
Shared runtime: support.js (design components), motion.js (animations), estimator.js, execution-demos.js, services-render.js.
Data: data/*.js, content/*.js. Netlify Functions in netlify/functions (content store, leads, feedback, robots).
